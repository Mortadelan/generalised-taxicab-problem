#include <stdio.h>
#include <math.h>
#include "lib.c"

int main(void) {
  generate_img(7);
  generate_img(11);
  generate_img(13);
  generate_img(17);
  generate_img(19);
  generate_img(20);
  generate_img(23);
  generate_img(31);
  return 0;
}
